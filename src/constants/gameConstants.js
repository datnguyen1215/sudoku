/**
 * Game constants for the Sudoku application
 */

// Game rules
export const MAX_MISTAKES = 3;
export const DEFAULT_HINTS = 3;

// Board dimensions
export const BOARD_SIZE = 9;
export const BOX_SIZE = 3;

// UI constants
export const DEFAULT_TIMER = '00:00';

// Default difficulty
export const DEFAULT_DIFFICULTY = {
  name: 'Easy',
  minClues: 30,
  maxClues: 35,
};
